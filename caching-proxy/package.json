{
  "name": "caching-proxy",
  "version": "1.0.0",
  "description": "A NodeJS CLI tool that starts a caching proxy server, it will forward requests to the actual server and cache the responses. If the same request is made again, it will return the cached response instead of forwarding the request to the server.",
  "main": "index.js",
  "bin": {
    "caching-proxy": "./src/index.js"
  },
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "caching-proxy": "node src/index.js",
    "test": "jest"
  },
  "keywords": ["node", "cli", "caching", "proxy", "server"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yargs": "^18.1.0"
  }
}
