# Folder info

A Node.js CLI tool that prints information about a folder.

The tool inspect the current folder by default. If the user passes a folder path, it should inspect that folder instead.

## Commands

```bash
npm run folder-info
npm run folder-info -- src
npm run folder-info -- missing-folder
```
